/**
 * Browser-safe product flags. Keep in sync with `@msfs-compat/shared`
 * (`BUSH_TRIPS_BOARD_ENABLED`) — Vite client must not import that package.
 */
export const BUSH_TRIPS_BOARD_ENABLED = false;
